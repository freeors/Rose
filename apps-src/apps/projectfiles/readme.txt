Windows
  'vc' directory is project files.
  'windows-prj' is used for Rose Studio, 1)New app's windows project. 2)generate app's Visual Studio project

Android
  'android' directory is project files.
  'android-prj' is used for Rose Studio, and generate app's android project.

kOS
  'kos' directory is project files.
  'kos-prj' is used for Rose Studio, and generate app's kos project.

iOS
  'Xcode-iOS' directory is project files.
  'ios-prj' is used for Rose Studio, and generate app's iOS project.